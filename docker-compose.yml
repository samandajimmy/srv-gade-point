version: '3'
services:
  db:
    image: postgres:9.6-alpine
    ports:
      - 5432:5432
    environment:
      - POSTGRES_HOST_AUTH_METHOD=trust

  app:
    build: .
    ports:
      - 8080:8080
    env_file:
      - .env